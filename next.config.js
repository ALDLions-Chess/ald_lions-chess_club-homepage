/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // If your repo is not at the root, set basePath and assetPrefix:
  // basePath: '/ald-chess_club-homepage',
  // assetPrefix: '/ald-chess_club-homepage/',
};

module.exports = nextConfig;
